import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.form.validation',
  appName: 'ionic-form-validation',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
