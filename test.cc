#include <iostream>
#include <vector>
using namespace std;
int main() {
	/*for (int i = 0; i < 3; i++) {
		for (int j = 0; j < 3; j++) {
			if (i == j) continue;
			cout << "I: " << i << "J: " << j << endl;
		}
	}
	*/
/*	vector<int> one {1,2,3};
	vector<int> two {4,5,6};
	for (int x : one) cout << x << " ";
	cout << endl;
	for (int x : two) cout << x << " ";
	cout << endl;
	one = one + two;
	cout << "NEW: ";
	for (int x : one) cout << x << " ";
	cout << endl;
*/
	int x = 0;
	cout << x << endl;
	while (true) {
		x+=1;
		cout << x << endl;
		break;
	}
	cout << x << endl;
}
